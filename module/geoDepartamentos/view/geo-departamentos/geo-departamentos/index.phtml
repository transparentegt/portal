<?php

 $title = 'Departamentos';
 $this->headTitle($title);
 ?>
<h1 class="tituloSeccion"><?php echo $this->escapeHtml($title); ?></h1>
<p class="menuSeccion">
     <a class="button" href="<?php echo $this->url('geoDepartamentos', array('action'=>'add'));?>" >
		<span>Agregar Departamento</span>
	</a>
</p>
<table class="table">
<tr>
    <th>ID</th>
    <th>Nombre</th>
    <th>&nbsp;</th>
</tr>
 <?php foreach ($this->paginator as $item) : ?>
 <tr>
 	<td><?php $item->id;?></td>
    <td><?php echo $this->escapeHtml($item->nombre);?> 	
    <td>
        <a href="<?php echo $this->url('geoDepartamentos',
            array('action'=>'edit', 'id' => $item->id));?>">Editar</a>
        <a href="<?php echo $this->url('geoDepartamentos',
            array('action'=>'delete', 'id' => $item->id));?>">Borrar</a>
    </td> 
 </tr>
 <?php endforeach; ?>
 </table>
  <?php
 // add at the end of the file after the table
 echo $this->paginationControl(
     // the paginator object
     $this->paginator,
     // the scrolling style
     'sliding',
     // the partial to use to render the control
     array('partial/paginator.phtml', 'geoDepartamentos'),
     // the route to link to when a user clicks a control link
     array(
         'route' => 'geoDepartamentos'
     )
 );
 ?>